[package]
name = "bevy-flake-template"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.release]
lto = true
opt-level = 'z'

[dependencies]
bevy = { version = "0.13", features = [ "wayland" ] }
